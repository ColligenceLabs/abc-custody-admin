# 관리자 계정 관리 시스템 설계 문서

## 📋 목차

1. [시스템 개요](#시스템-개요)
2. [데이터베이스 설계](#데이터베이스-설계)
3. [API 엔드포인트 설계](#api-엔드포인트-설계)
4. [UI/UX 화면 설계](#uiux-화면-설계)
5. [컴포넌트 구조](#컴포넌트-구조)
6. [보안 고려사항](#보안-고려사항)
7. [구현 로드맵](#구현-로드맵)

---

## 시스템 개요

### 목적
관리자 계정의 생성, 수정, 삭제 및 권한 관리를 위한 SUPER_ADMIN 전용 관리 시스템

### 주요 기능
- 관리자 계정 CRUD (생성, 조회, 수정, 삭제)
- 역할 기반 접근 제어 (RBAC)
- 세밀한 권한 커스터마이징
- 활성 세션 모니터링 및 관리
- 감사 로그 조회 및 내보내기
- 2FA 활성화/비활성화
- IP 화이트리스트 관리

### 접근 권한
- SUPER_ADMIN 역할만 접근 가능
- 다른 역할은 메뉴 자체가 표시되지 않음

---

## 데이터베이스 설계

### 1. admin_users (관리자 계정)

```sql
CREATE TABLE admin_users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  name VARCHAR(100) NOT NULL,
  role VARCHAR(50) NOT NULL CHECK (role IN ('super_admin', 'operations', 'compliance', 'support', 'viewer')),
  status VARCHAR(50) NOT NULL CHECK (status IN ('active', 'suspended', 'inactive', 'pending')) DEFAULT 'pending',
  two_factor_enabled BOOLEAN DEFAULT false,
  two_factor_secret VARCHAR(255),
  ip_whitelist JSONB DEFAULT '[]',
  session_timeout INTEGER DEFAULT 30,
  last_login TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  created_by UUID REFERENCES admin_users(id),
  notes TEXT,
  CONSTRAINT valid_email CHECK (email ~* '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$')
);

-- 인덱스
CREATE INDEX idx_admin_users_email ON admin_users(email);
CREATE INDEX idx_admin_users_role ON admin_users(role);
CREATE INDEX idx_admin_users_status ON admin_users(status);
CREATE INDEX idx_admin_users_created_at ON admin_users(created_at DESC);
```

### 2. admin_sessions (활성 세션)

```sql
CREATE TABLE admin_sessions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES admin_users(id) ON DELETE CASCADE,
  access_token VARCHAR(500) UNIQUE NOT NULL,
  refresh_token VARCHAR(500) UNIQUE NOT NULL,
  expires_at TIMESTAMP NOT NULL,
  ip_address VARCHAR(45) NOT NULL,
  user_agent TEXT,
  last_activity TIMESTAMP DEFAULT NOW(),
  is_active BOOLEAN DEFAULT true,
  login_method VARCHAR(50) CHECK (login_method IN ('password', 'password_2fa', 'sso')),
  created_at TIMESTAMP DEFAULT NOW(),
  CONSTRAINT valid_expiry CHECK (expires_at > created_at)
);

-- 인덱스
CREATE INDEX idx_admin_sessions_user_id ON admin_sessions(user_id);
CREATE INDEX idx_admin_sessions_access_token ON admin_sessions(access_token);
CREATE INDEX idx_admin_sessions_expires_at ON admin_sessions(expires_at);
CREATE INDEX idx_admin_sessions_is_active ON admin_sessions(is_active);

-- 만료된 세션 자동 정리 (PostgreSQL 함수)
CREATE OR REPLACE FUNCTION cleanup_expired_sessions()
RETURNS void AS $$
BEGIN
  UPDATE admin_sessions
  SET is_active = false
  WHERE expires_at < NOW() AND is_active = true;
END;
$$ LANGUAGE plpgsql;
```

### 3. admin_permissions (개별 권한 오버라이드)

```sql
CREATE TABLE admin_permissions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES admin_users(id) ON DELETE CASCADE,
  resource VARCHAR(50) NOT NULL CHECK (resource IN ('members', 'vault', 'withdrawals', 'deposits', 'compliance', 'reports', 'settings', 'admin_users')),
  actions JSONB NOT NULL DEFAULT '[]',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  CONSTRAINT unique_user_resource UNIQUE(user_id, resource),
  CONSTRAINT valid_actions CHECK (jsonb_typeof(actions) = 'array')
);

-- 인덱스
CREATE INDEX idx_admin_permissions_user_id ON admin_permissions(user_id);
CREATE INDEX idx_admin_permissions_resource ON admin_permissions(resource);
```

### 4. admin_audit_logs (감사 로그)

```sql
CREATE TABLE admin_audit_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES admin_users(id) ON DELETE SET NULL,
  user_name VARCHAR(100) NOT NULL,
  user_email VARCHAR(255) NOT NULL,
  user_role VARCHAR(50) NOT NULL,
  action VARCHAR(100) NOT NULL,
  resource VARCHAR(100) NOT NULL,
  resource_id VARCHAR(255),
  details JSONB DEFAULT '{}',
  ip_address VARCHAR(45) NOT NULL,
  user_agent TEXT,
  result VARCHAR(50) CHECK (result IN ('success', 'failure')) DEFAULT 'success',
  error_message TEXT,
  timestamp TIMESTAMP DEFAULT NOW(),
  retention_until TIMESTAMP,
  CONSTRAINT valid_retention CHECK (retention_until IS NULL OR retention_until > timestamp)
);

-- 인덱스
CREATE INDEX idx_audit_logs_user_id ON admin_audit_logs(user_id);
CREATE INDEX idx_audit_logs_action ON admin_audit_logs(action);
CREATE INDEX idx_audit_logs_resource ON admin_audit_logs(resource);
CREATE INDEX idx_audit_logs_timestamp ON admin_audit_logs(timestamp DESC);
CREATE INDEX idx_audit_logs_result ON admin_audit_logs(result);

-- 파티셔닝 (성능 최적화, 월별)
-- CREATE TABLE admin_audit_logs_2025_01 PARTITION OF admin_audit_logs
-- FOR VALUES FROM ('2025-01-01') TO ('2025-02-01');
```

### 5. Mock JSON DB 구조 (개발용)

```typescript
// src/data/mockData/adminUsers.json
{
  "adminUsers": [
    {
      "id": "admin-001",
      "email": "super@custody.com",
      "name": "슈퍼 관리자",
      "role": "super_admin",
      "status": "active",
      "twoFactorEnabled": true,
      "ipWhitelist": [],
      "sessionTimeout": 30,
      "lastLogin": "2025-01-09T10:00:00Z",
      "createdAt": "2024-01-01T00:00:00Z",
      "updatedAt": "2025-01-09T10:00:00Z",
      "createdBy": null,
      "notes": "시스템 기본 관리자"
    }
  ],
  "adminSessions": [],
  "adminPermissions": [],
  "adminAuditLogs": []
}
```

---

## API 엔드포인트 설계

### Admin Users API

#### GET /api/admin/users
관리자 목록 조회 (필터, 정렬, 페이징)

**Query Parameters:**
```typescript
{
  page?: number;          // 페이지 번호 (기본: 1)
  limit?: number;         // 페이지당 항목 수 (기본: 20)
  role?: AdminRole[];     // 역할 필터
  status?: AdminUserStatus[]; // 상태 필터
  search?: string;        // 이름/이메일 검색
  sortBy?: 'name' | 'email' | 'role' | 'lastLogin' | 'createdAt';
  sortOrder?: 'asc' | 'desc';
}
```

**Response:**
```typescript
{
  data: AdminUser[];
  pagination: {
    total: number;
    page: number;
    limit: number;
    totalPages: number;
  };
}
```

#### POST /api/admin/users
새 관리자 생성

**Request Body:**
```typescript
{
  email: string;
  name: string;
  role: AdminRole;
  twoFactorRequired?: boolean;
  ipWhitelist?: string[];
  sessionTimeout?: number;
  notes?: string;
}
```

**Response:**
```typescript
{
  user: AdminUser;
  tempPassword: string; // 임시 비밀번호
}
```

#### GET /api/admin/users/:id
특정 관리자 상세 조회

**Response:**
```typescript
{
  user: AdminUser;
  permissions: AdminPermission[];
  activeSessions: AdminSession[];
  stats: {
    totalLogins: number;
    lastLoginIp: string;
    accountAge: number; // days
  };
}
```

#### PUT /api/admin/users/:id
관리자 정보 수정

**Request Body:**
```typescript
{
  name?: string;
  role?: AdminRole;
  status?: AdminUserStatus;
  twoFactorEnabled?: boolean;
  ipWhitelist?: string[];
  sessionTimeout?: number;
  notes?: string;
}
```

#### PATCH /api/admin/users/:id/status
상태 변경 (활성화/비활성화)

**Request Body:**
```typescript
{
  status: AdminUserStatus;
  reason?: string; // 비활성화 사유
}
```

#### DELETE /api/admin/users/:id
관리자 삭제 (Soft Delete)

**Query Parameters:**
```typescript
{
  force?: boolean; // true면 Hard Delete
}
```

#### GET /api/admin/users/:id/permissions
권한 조회

**Response:**
```typescript
{
  rolePermissions: AdminPermission[]; // 역할 기본 권한
  customPermissions: AdminPermission[]; // 커스텀 권한
  effectivePermissions: AdminPermission[]; // 최종 적용 권한
}
```

#### PUT /api/admin/users/:id/permissions
권한 수정

**Request Body:**
```typescript
{
  permissions: Array<{
    resource: AdminResource;
    actions: AdminAction[];
  }>;
}
```

#### POST /api/admin/users/:id/reset-password
비밀번호 재설정

**Response:**
```typescript
{
  tempPassword: string;
  expiresAt: string; // ISO 8601
}
```

#### POST /api/admin/users/:id/enable-2fa
2FA 활성화

**Response:**
```typescript
{
  qrCodeUrl: string;
  secret: string;
  backupCodes: string[];
}
```

#### POST /api/admin/users/:id/disable-2fa
2FA 비활성화

**Request Body:**
```typescript
{
  currentPassword: string; // 본인 확인
}
```

### Admin Sessions API

#### GET /api/admin/sessions
전체 활성 세션 조회

**Query Parameters:**
```typescript
{
  userId?: string;
  ipAddress?: string;
  isActive?: boolean;
  page?: number;
  limit?: number;
}
```

#### DELETE /api/admin/sessions/:id
세션 강제 종료

#### DELETE /api/admin/users/:userId/sessions
특정 관리자의 모든 세션 종료

### Admin Audit Logs API

#### GET /api/admin/audit-logs
감사 로그 조회

**Query Parameters:**
```typescript
{
  userId?: string;
  action?: string[];
  resource?: string[];
  result?: 'success' | 'failure';
  startDate?: string; // ISO 8601
  endDate?: string;
  page?: number;
  limit?: number;
}
```

#### GET /api/admin/audit-logs/:id
특정 로그 상세

#### POST /api/admin/audit-logs/export
CSV 내보내기

**Request Body:**
```typescript
{
  filters: AuditLogFilter;
  format: 'csv' | 'json' | 'xlsx';
}
```

**Response:**
```typescript
{
  downloadUrl: string;
  expiresAt: string;
}
```

### Statistics API

#### GET /api/admin/stats/overview
관리자 통계

**Response:**
```typescript
{
  totalAdmins: number;
  activeAdmins: number;
  activeSessions: number;
  byRole: Record<AdminRole, number>;
  byStatus: Record<AdminUserStatus, number>;
  recentActivity: {
    last24h: number;
    last7d: number;
    last30d: number;
  };
}
```

---

## UI/UX 화면 설계

### 1. 관리자 목록 화면 (/admin/system/admins)

**레이아웃:**
```
┌─────────────────────────────────────────────────────────┐
│ 시스템 관리 > 관리자 계정                              [+ 새 관리자 추가] │
├─────────────────────────────────────────────────────────┤
│ 필터: [역할: 전체 ▼] [상태: 전체 ▼] [🔍 검색]               │
├─────────────────────────────────────────────────────────┤
│ 이름        이메일            역할        상태    2FA  마지막 로그인   액션 │
│ ─────────────────────────────────────────────────────── │
│ 슈퍼관리자  super@custody.com  SUPER_ADMIN  활성  ✓   2시간 전    [수정][로그] │
│ 운영팀      ops@custody.com    OPERATIONS   활성  ✓   1일 전     [수정][로그] │
│ 컴플라이언스 comp@custody.com   COMPLIANCE   활성  -   3일 전     [수정][로그] │
│ ─────────────────────────────────────────────────────── │
│ 페이지: [1] 2 3 ... 10                        총 47명    │
└─────────────────────────────────────────────────────────┘
```

**주요 기능:**
- 역할별, 상태별 필터 (Select 드롭다운)
- 이름/이메일 실시간 검색
- 테이블 컬럼별 정렬
- 페이지네이션 (20개씩)
- 액션 버튼: 수정, 활동 로그, 비활성화

**색상 코드:**
- 역할 배지:
  - SUPER_ADMIN: Sapphire 그라데이션
  - OPERATIONS: Blue
  - COMPLIANCE: Purple
  - SUPPORT: Green
  - VIEWER: Gray
- 상태 배지:
  - ACTIVE: Green
  - SUSPENDED: Orange
  - INACTIVE: Gray
  - PENDING: Yellow

### 2. 관리자 생성 화면 (/admin/system/admins/new)

**레이아웃:**
```
┌─────────────────────────────────────────────────────────┐
│ 시스템 관리 > 관리자 계정 > 새 관리자 추가                          │
├─────────────────────────────────────────────────────────┤
│ 기본 정보                                                │
│ ─────────────────────────────────────────────────────── │
│ 이메일 *        [                              ]          │
│                 example@custody.com                      │
│                                                          │
│ 이름 *          [                              ]          │
│                 관리자 이름을 입력하세요                    │
│                                                          │
│ 역할 *          [OPERATIONS              ▼]              │
│                 선택한 역할에 따라 기본 권한이 부여됩니다      │
│                                                          │
│ 보안 설정                                                │
│ ─────────────────────────────────────────────────────── │
│ □ 2단계 인증 활성화                                       │
│   (최초 로그인 시 QR 코드 제공)                            │
│                                                          │
│ 세션 타임아웃   [30] 분                                   │
│                                                          │
│ IP 화이트리스트 (선택)                                    │
│ [192.168.1.1        ] [+ 추가]                           │
│ [10.0.0.0/24        ] [삭제]                             │
│                                                          │
│ 비고                                                     │
│ ┌────────────────────────────────────────┐               │
│ │                                        │               │
│ │                                        │               │
│ └────────────────────────────────────────┘               │
│                                                          │
│              [취소]  [관리자 생성]                        │
└─────────────────────────────────────────────────────────┘
```

**유효성 검증:**
- 이메일: 형식 검증, 중복 확인
- 이름: 2-50자
- 역할: 필수 선택
- IP 주소: CIDR 표기법 지원 (예: 192.168.1.0/24)
- 세션 타임아웃: 5-120분

**생성 시 동작:**
1. 임시 비밀번호 자동 생성
2. 이메일 발송 (임시 비밀번호 + 로그인 링크)
3. 감사 로그 기록
4. Toast 알림: "관리자가 생성되었습니다. 임시 비밀번호가 이메일로 발송되었습니다."

### 3. 관리자 상세/수정 화면 (/admin/system/admins/[id])

**탭 구조:**
```
┌─────────────────────────────────────────────────────────┐
│ 시스템 관리 > 관리자 계정 > 슈퍼 관리자                           │
├─────────────────────────────────────────────────────────┤
│ [기본 정보] [권한 설정] [보안 설정] [활성 세션] [활동 로그]      │
├─────────────────────────────────────────────────────────┤
│ ... 탭별 콘텐츠 ...                                       │
└─────────────────────────────────────────────────────────┘
```

#### 3-1. 기본 정보 탭

```
이메일          super@custody.com
이름            [슈퍼 관리자                    ]
역할            [SUPER_ADMIN              ▼]
상태            [활성                      ▼]
마지막 로그인    2025-01-09 10:30:15 (2시간 전)
계정 생성일      2024-01-01 00:00:00
생성자          시스템

비고
┌────────────────────────────────────────┐
│ 시스템 기본 관리자                      │
└────────────────────────────────────────┘

[비밀번호 재설정] [변경사항 저장]
```

#### 3-2. 권한 설정 탭 (Permission Matrix)

```
리소스별 세부 권한 설정

┌──────────────┬─────┬─────┬─────┬─────┬─────┬─────┬─────┬─────┐
│ 리소스       │ 조회│ 생성│ 수정│ 삭제│ 승인│ 반려│ 정지│ 활성│
├──────────────┼─────┼─────┼─────┼─────┼─────┼─────┼─────┼─────┤
│ 회원사       │ ✓  │ ✓  │ ✓  │ ✓  │ ✓  │ ✓  │ ✓  │ ✓  │
│ 볼트         │ ✓  │ ✓  │ ✓  │ ✓  │ -   │ -   │ -   │ -   │
│ 출금         │ ✓  │ ✓  │ ✓  │ ✓  │ ✓  │ ✓  │ -   │ -   │
│ 입금         │ ✓  │ ✓  │ ✓  │ ✓  │ -   │ -   │ -   │ -   │
│ 컴플라이언스 │ ✓  │ ✓  │ ✓  │ ✓  │ ✓  │ ✓  │ -   │ -   │
│ 보고서       │ ✓  │ ✓  │ -   │ -   │ -   │ -   │ -   │ -   │
│ 시스템 설정  │ ✓  │ -   │ ✓  │ -   │ -   │ -   │ -   │ -   │
│ 관리자 계정  │ ✓  │ ✓  │ ✓  │ ✓  │ -   │ -   │ ✓  │ ✓  │
└──────────────┴─────┴─────┴─────┴─────┴─────┴─────┴─────┴─────┘

역할 기본 권한: SUPER_ADMIN (모든 권한)
커스텀 권한: 없음

[기본값으로 재설정] [변경사항 저장]
```

#### 3-3. 보안 설정 탭

```
2단계 인증 (2FA)
상태: 활성화됨 ✓
백업 코드: 3개 남음
[백업 코드 재생성] [2FA 비활성화]

IP 화이트리스트
□ IP 화이트리스트 사용
  (비활성화 시 모든 IP에서 접근 가능)

허용된 IP 주소:
[192.168.1.100      ] [삭제]
[10.0.0.0/24        ] [삭제]
[+ IP 주소 추가]

세션 설정
세션 타임아웃: [30] 분
  (5-120분 사이 설정 가능)

[변경사항 저장]
```

#### 3-4. 활성 세션 탭

```
현재 활성 세션: 2개

┌──────────────────────────────────────────────────────────┐
│ IP 주소         로그인 시간          마지막 활동      만료 시간   액션 │
├──────────────────────────────────────────────────────────┤
│ 192.168.1.100  2025-01-09 10:30:15  2분 전         28분 후  [강제 종료] │
│                Chrome 120.0 / Windows 10                  │
│ ─────────────────────────────────────────────────────────│
│ 10.0.0.50      2025-01-09 09:15:22  1시간 전       만료됨  [삭제] │
│                Safari 17.0 / macOS                        │
└──────────────────────────────────────────────────────────┘

[모든 세션 강제 종료]
```

#### 3-5. 활동 로그 탭

```
최근 활동 로그 (최근 30일)

필터: [액션: 전체 ▼] [결과: 전체 ▼] [날짜 범위: 최근 30일 ▼]

┌──────────────────────────────────────────────────────────┐
│ 시간                액션           리소스         결과    상세 │
├──────────────────────────────────────────────────────────┤
│ 2025-01-09 10:30:15  로그인        인증           성공   [보기] │
│ 2025-01-09 10:28:42  관리자 수정   admin_users    성공   [보기] │
│ 2025-01-09 09:15:30  회원사 조회   members        성공   [보기] │
│ 2025-01-08 16:45:12  출금 승인     withdrawals    성공   [보기] │
│ 2025-01-08 14:20:05  로그인 실패   인증           실패   [보기] │
└──────────────────────────────────────────────────────────┘

[CSV 내보내기]
```

### 4. 활성 세션 관리 화면 (/admin/system/sessions)

```
┌─────────────────────────────────────────────────────────┐
│ 시스템 관리 > 활성 세션                                      │
├─────────────────────────────────────────────────────────┤
│ 필터: [관리자: 전체 ▼] [의심스러운 활동만 □]                  │
├─────────────────────────────────────────────────────────┤
│ 관리자      IP 주소         로그인 시간      마지막 활동  만료  액션 │
│ ─────────────────────────────────────────────────────── │
│ 슈퍼관리자  192.168.1.100  10:30:15      2분 전     28분 [종료] │
│             Chrome / Windows                                 │
│ ─────────────────────────────────────────────────────── │
│ 운영팀      203.0.113.1 ⚠ 09:20:12      5분 전     25분 [종료] │
│             Safari / macOS (의심스러운 국가: 베트남)          │
│ ─────────────────────────────────────────────────────── │
│ 컴플라이언스 10.0.0.25      08:15:30      1시간 전   만료 [삭제] │
│             Firefox / Linux                                  │
└─────────────────────────────────────────────────────────┘

의심스러운 활동 감지 기준:
- 평소와 다른 국가에서 접속
- 짧은 시간 내 여러 IP에서 로그인
- 브루트 포스 시도 감지
```

### 5. 감사 로그 화면 (/admin/system/audit-logs)

```
┌─────────────────────────────────────────────────────────┐
│ 시스템 관리 > 감사 로그                                      │
├─────────────────────────────────────────────────────────┤
│ 관리자: [전체 ▼]  액션: [전체 ▼]  결과: [전체 ▼]            │
│ 기간: [2025-01-01] ~ [2025-01-09]            [CSV 내보내기] │
├─────────────────────────────────────────────────────────┤
│ 시간         관리자        액션           리소스      결과   상세 │
│ ─────────────────────────────────────────────────────── │
│ 10:30:15    슈퍼관리자    로그인         인증        성공  [보기] │
│ 10:28:42    슈퍼관리자    관리자 수정    admin_users 성공  [보기] │
│ 09:20:15    운영팀        출금 승인      withdrawals 성공  [보기] │
│ 08:45:30    컴플라이언스  AML 검토      compliance  성공  [보기] │
│ 08:12:22    운영팀        로그인 실패    인증        실패  [보기] │
└─────────────────────────────────────────────────────────┘
```

**로그 상세 모달:**
```
┌─────────────────────────────────────────────────────────┐
│ 감사 로그 상세                                      [✕ 닫기] │
├─────────────────────────────────────────────────────────┤
│ 시간:     2025-01-09 10:28:42                             │
│ 관리자:   슈퍼 관리자 (super@custody.com)                  │
│ 역할:     SUPER_ADMIN                                     │
│ 액션:     관리자 수정                                      │
│ 리소스:   admin_users                                     │
│ 대상 ID:  admin-002                                       │
│ 결과:     성공                                            │
│ IP 주소:  192.168.1.100                                   │
│                                                          │
│ 변경 내역:                                                │
│ ┌────────────────────────────────────────┐               │
│ │ 필드: role                             │               │
│ │ 이전: operations                       │               │
│ │ 이후: compliance                       │               │
│ │                                        │               │
│ │ 필드: status                           │               │
│ │ 이전: pending                          │               │
│ │ 이후: active                           │               │
│ └────────────────────────────────────────┘               │
│                                                          │
│                        [확인]                            │
└─────────────────────────────────────────────────────────┘
```

---

## 컴포넌트 구조

### 디렉토리 구조

```
src/
├── app/
│   └── admin/
│       └── system/
│           ├── layout.tsx                    # 시스템 관리 레이아웃
│           ├── page.tsx                      # 시스템 관리 대시보드
│           ├── admins/
│           │   ├── page.tsx                  # 관리자 목록
│           │   ├── new/
│           │   │   └── page.tsx              # 신규 생성
│           │   └── [id]/
│           │       ├── page.tsx              # 상세/수정
│           │       └── activity/
│           │           └── page.tsx          # 활동 로그
│           ├── sessions/
│           │   └── page.tsx                  # 활성 세션
│           └── audit-logs/
│               └── page.tsx                  # 감사 로그
│
├── components/
│   └── admin/
│       └── system/
│           ├── AdminUserTable.tsx            # 관리자 목록 테이블
│           ├── AdminUserTableRow.tsx         # 테이블 행 컴포넌트
│           ├── AdminUserFilters.tsx          # 필터 컴포넌트
│           ├── AdminUserForm.tsx             # 생성/수정 폼
│           ├── AdminUserDetailTabs.tsx       # 상세 페이지 탭
│           ├── BasicInfoTab.tsx              # 기본 정보 탭
│           ├── PermissionsTab.tsx            # 권한 설정 탭
│           ├── SecurityTab.tsx               # 보안 설정 탭
│           ├── SessionsTab.tsx               # 활성 세션 탭
│           ├── ActivityTab.tsx               # 활동 로그 탭
│           ├── PermissionMatrix.tsx          # 권한 매트릭스
│           ├── ActiveSessionsTable.tsx       # 활성 세션 테이블
│           ├── AuditLogTable.tsx             # 감사 로그 테이블
│           ├── AuditLogDetailModal.tsx       # 로그 상세 모달
│           ├── RoleBadge.tsx                 # 역할 배지
│           ├── StatusBadge.tsx               # 상태 배지
│           ├── SessionTimeoutConfig.tsx      # 세션 타임아웃 설정
│           ├── IPWhitelistManager.tsx        # IP 화이트리스트 관리
│           └── TwoFactorSettings.tsx         # 2FA 설정
│
├── services/
│   ├── adminUserApi.ts                       # 관리자 CRUD API
│   ├── adminSessionApi.ts                    # 세션 관리 API
│   ├── adminAuditLogApi.ts                   # 감사 로그 API
│   └── adminStatsApi.ts                      # 통계 API
│
├── types/
│   └── admin.ts                              # 타입 정의 (기존 확장)
│
└── hooks/
    ├── useAdminUsers.ts                      # 관리자 목록 훅
    ├── useAdminUser.ts                       # 단일 관리자 훅
    ├── useAdminSessions.ts                   # 세션 관리 훅
    └── useAuditLogs.ts                       # 감사 로그 훅
```

### 주요 컴포넌트 명세

#### 1. AdminUserTable.tsx

```typescript
interface AdminUserTableProps {
  users: AdminUser[];
  onEdit: (userId: string) => void;
  onViewActivity: (userId: string) => void;
  onStatusChange: (userId: string, status: AdminUserStatus) => void;
}

// 기능:
// - 테이블 렌더링
// - 정렬 처리
// - 페이지네이션
// - 액션 버튼 이벤트
```

#### 2. AdminUserForm.tsx

```typescript
interface AdminUserFormProps {
  mode: 'create' | 'edit';
  initialData?: Partial<AdminUser>;
  onSubmit: (data: CreateAdminUserRequest) => Promise<void>;
  onCancel: () => void;
}

// 기능:
// - React Hook Form 사용
// - Zod 스키마 유효성 검증
// - 이메일 중복 확인
// - IP 주소 유효성 검증
```

#### 3. PermissionMatrix.tsx

```typescript
interface PermissionMatrixProps {
  userId: string;
  rolePermissions: AdminPermission[];
  customPermissions: AdminPermission[];
  onSave: (permissions: AdminPermission[]) => Promise<void>;
}

// 기능:
// - 체크박스 매트릭스 렌더링
// - 역할 기본 권한 표시 (비활성화)
// - 커스텀 권한 수정
// - 변경사항 추적
```

#### 4. ActiveSessionsTable.tsx

```typescript
interface ActiveSessionsTableProps {
  sessions: AdminSession[];
  onTerminate: (sessionId: string) => Promise<void>;
  highlightSuspicious?: boolean;
}

// 기능:
// - 세션 목록 표시
// - 의심스러운 세션 하이라이트
// - 강제 종료 기능
// - 자동 갱신 (30초마다)
```

#### 5. AuditLogTable.tsx

```typescript
interface AuditLogTableProps {
  logs: AdminAuditLog[];
  onViewDetail: (logId: string) => void;
  onExport: (filters: AuditLogFilter) => void;
}

// 기능:
// - 로그 목록 표시
// - 필터링
// - 상세 보기
// - CSV 내보내기
```

---

## 보안 고려사항

### 1. 비밀번호 정책
- 최소 12자 이상
- 대문자, 소문자, 숫자, 특수문자 각 1개 이상 포함
- bcrypt 해싱 (cost factor: 12)
- 임시 비밀번호 유효 기간: 24시간

### 2. 세션 관리
- JWT 토큰 사용 (HS256 알고리즘)
- Access Token: 30분 유효
- Refresh Token: 7일 유효
- 토큰 블랙리스트 관리 (Redis)
- 세션 고정 공격 방지

### 3. 2FA (Two-Factor Authentication)
- TOTP (Time-based One-Time Password) 사용
- QR 코드 생성 (otplib 라이브러리)
- 백업 코드 제공 (10개, 1회용)
- 2FA 복구 절차

### 4. IP 화이트리스트
- CIDR 표기법 지원
- IPv4 및 IPv6 지원
- IP 범위 검증
- 화이트리스트 우회 방지

### 5. 감사 로그
- 모든 관리자 액션 기록
- 변경 전후 상태 저장
- 보관 기간: 7년 (법적 요구사항)
- 로그 무결성 검증 (해시 체인)

### 6. 접근 제어
- SUPER_ADMIN만 시스템 관리 접근
- API 레벨 권한 검증
- UI 레벨 권한 검증
- 본인 계정 비활성화 방지

### 7. 보안 헤더
```typescript
// Next.js middleware
export const securityHeaders = {
  'X-Frame-Options': 'DENY',
  'X-Content-Type-Options': 'nosniff',
  'X-XSS-Protection': '1; mode=block',
  'Strict-Transport-Security': 'max-age=31536000; includeSubDomains',
  'Content-Security-Policy': "default-src 'self'",
};
```

---

## 구현 로드맵

### Phase 1: 기반 작업 (1주)
- [ ] PostgreSQL 스키마 마이그레이션 작성
- [ ] Mock JSON DB 구조 생성
- [ ] 초기 시드 데이터 (샘플 관리자 3-5명)
- [ ] 타입 정의 확장 (admin.ts)

### Phase 2: API 서비스 레이어 (1-2주)
- [ ] adminUserApi.ts 구현
  - [ ] CRUD 엔드포인트
  - [ ] 권한 관리 엔드포인트
  - [ ] 비밀번호 재설정
- [ ] adminSessionApi.ts 구현
  - [ ] 세션 조회 및 관리
  - [ ] 강제 종료 기능
- [ ] adminAuditLogApi.ts 구현
  - [ ] 로그 조회 및 필터링
  - [ ] CSV 내보내기
- [ ] Zod 스키마 유효성 검증
- [ ] 에러 핸들링

### Phase 3: 공통 UI 컴포넌트 (1주)
- [ ] RoleBadge.tsx (역할 배지)
- [ ] StatusBadge.tsx (상태 배지)
- [ ] IPWhitelistManager.tsx (IP 관리)
- [ ] SessionTimeoutConfig.tsx (타임아웃 설정)
- [ ] TwoFactorSettings.tsx (2FA 설정)

### Phase 4: 관리자 목록 및 필터링 (1주)
- [ ] AdminUserTable.tsx (테이블)
- [ ] AdminUserTableRow.tsx (행)
- [ ] AdminUserFilters.tsx (필터)
- [ ] 정렬 및 페이지네이션
- [ ] 검색 기능

### Phase 5: 관리자 생성 기능 (1주)
- [ ] AdminUserForm.tsx (폼)
- [ ] 유효성 검증
- [ ] 이메일 중복 확인
- [ ] 임시 비밀번호 생성
- [ ] 이메일 발송 (모의)

### Phase 6: 관리자 상세 및 수정 (1주)
- [ ] AdminUserDetailTabs.tsx (탭 컨테이너)
- [ ] BasicInfoTab.tsx (기본 정보)
- [ ] SecurityTab.tsx (보안 설정)
- [ ] 수정 기능
- [ ] 비밀번호 재설정

### Phase 7: 권한 관리 (1주)
- [ ] PermissionsTab.tsx (권한 탭)
- [ ] PermissionMatrix.tsx (매트릭스)
- [ ] 역할 기본 권한 표시
- [ ] 커스텀 권한 수정
- [ ] 변경사항 저장

### Phase 8: 세션 관리 (1주)
- [ ] SessionsTab.tsx (세션 탭)
- [ ] ActiveSessionsTable.tsx (세션 테이블)
- [ ] 전체 세션 관리 페이지
- [ ] 강제 종료 기능
- [ ] 의심스러운 세션 감지

### Phase 9: 감사 로그 (1주)
- [ ] ActivityTab.tsx (활동 탭)
- [ ] AuditLogTable.tsx (로그 테이블)
- [ ] AuditLogDetailModal.tsx (상세 모달)
- [ ] 전체 감사 로그 페이지
- [ ] 필터링 및 검색
- [ ] CSV 내보내기

### Phase 10: 보안 강화 (1주)
- [ ] 2FA 활성화/비활성화
- [ ] QR 코드 생성
- [ ] 백업 코드 관리
- [ ] IP 화이트리스트 검증
- [ ] 세션 타임아웃 자동 처리

### Phase 11: 통계 및 대시보드 (1주)
- [ ] 시스템 관리 대시보드
- [ ] 관리자 통계
- [ ] 활동 차트
- [ ] 최근 활동 요약

### Phase 12: 테스트 및 최적화 (1주)
- [ ] 단위 테스트
- [ ] 통합 테스트
- [ ] 성능 최적화
- [ ] 접근성 검증
- [ ] 문서화

**총 예상 기간: 11-13주**

---

## 추가 고려사항

### 1. 이메일 알림
- 새 관리자 생성 시 임시 비밀번호 발송
- 비밀번호 재설정 시 알림
- 의심스러운 활동 감지 시 알림
- 계정 비활성화 시 알림

### 2. 다국어 지원
- 한국어 우선
- 영어 지원 준비
- i18n 라이브러리 사용

### 3. 모바일 대응
- 반응형 테이블 (스크롤)
- 모바일 필터 UI
- 터치 최적화

### 4. 성능 최적화
- 가상 스크롤 (대량 데이터)
- 무한 스크롤 또는 페이지네이션
- 메모이제이션
- 디바운싱 (검색)

### 5. 접근성 (a11y)
- WCAG 2.1 AA 준수
- 키보드 네비게이션
- 스크린 리더 지원
- ARIA 라벨

---

## 참고 자료

### 관련 문서
- `claudedocs/admin-panel-implementation-plan.md` - 전체 비즈니스 플로우
- `src/types/admin.ts` - 기존 타입 정의
- `src/contexts/AdminAuthContext.tsx` - 인증 컨텍스트

### 라이브러리
- **React Hook Form**: 폼 관리
- **Zod**: 스키마 유효성 검증
- **otplib**: TOTP 구현
- **qrcode**: QR 코드 생성
- **bcrypt**: 비밀번호 해싱
- **jsonwebtoken**: JWT 생성/검증

### 보안 표준
- OWASP Top 10
- NIST 비밀번호 가이드라인
- ISO 27001

---

**문서 버전**: 1.0
**작성일**: 2025-01-09
**작성자**: AI Design Assistant
