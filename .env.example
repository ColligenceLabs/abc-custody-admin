# API URL (프록시 사용으로 빈 문자열 설정)
# next.config.js의 rewrites가 /api/* 요청을 백엔드로 프록시합니다
NEXT_PUBLIC_API_URL=

# 프로덕션 백엔드 URL (프록시 대상)
NEXT_PUBLIC_BACKEND_URL=https://api.example.com

# WebSocket URL (Socket.IO는 루트 경로에서 실행)
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000

# Blockchain Environment (testnet | mainnet)
NEXT_PUBLIC_BLOCKCHAIN_ENV=testnet

# Development Settings
NODE_ENV=development
